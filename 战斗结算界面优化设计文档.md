# 战斗结算界面优化设计文档

**文档版本**：v1.0  
**创建日期**：2025-10-23  
**文档类型**：功能策划 + UI设计文档  
**所属模块**：核心玩法 - 战斗系统  

---

## 一、文档概述

### 1.1 设计目标

优化战斗结算界面，为玩家提供更详细、更有价值的战斗数据反馈，增强游戏的策略性和成就感。

**核心价值**：
- **成就反馈**：胜利时突出个人贡献和MVP，增强正向激励
- **失败分析**：失败时提供死因分析，帮助玩家改进策略
- **数据可视化**：直观的数据展示，降低理解成本
- **策略指导**：通过数据引导玩家优化阵容和战术

### 1.2 设计范围

本文档涵盖：
- 胜利结算界面重设计（数据统计、MVP机制、UI布局）
- 失败结算界面重设计（伤害来源分析、死因统计、UI布局）
- 数据收集和计算逻辑
- 视觉设计规范
- 交互细节

---

## 二、胜利结算界面设计

### 2.1 核心功能设计

#### 2.1.1 数据统计项

**角色伤害统计**（必须记录）：
- 角色名称
- 角色元素/职业
- 造成总伤害值
- 伤害占比（百分比）
- 击杀数（如有）
- 技能释放次数

**MVP评选规则**：
- **判定标准**：造成伤害最高的角色
- **次要指标**（平局时）：击杀数 > 技能释放次数 > 存活时长
- **特殊标识**：MVP角色获得金色边框和特殊标记

#### 2.1.2 展示优先级

**信息层级**：
```
第一层：胜利标题 + 基础信息（用时、获得奖励）
第二层：角色伤害统计（核心数据）
第三层：MVP突出展示
第四层：操作按钮
```

---

### 2.2 UI布局设计（胜利界面）

#### 2.2.1 整体布局结构

```
┌─────────────────────────────────────────────────────────┐
│                     🎉 胜利！                            │
│                 战斗用时：45秒 | 剩余HP：80%              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌─────────────────────────────────────────────┐     │
│   │         👑 本场MVP：烈焰战士                 │     │
│   │     造成伤害：850 (45% 总输出)               │     │
│   └─────────────────────────────────────────────┘     │
│                                                         │
│   ┌───────────────────────────────────────────────┐   │
│   │  【英雄战绩】                                  │   │
│   ├───────────────────────────────────────────────┤   │
│   │  1️⃣ 烈焰战士 🔥               MVP 👑         │   │
│   │     伤害: 850 (45%)  击杀: 2  技能: 8次      │   │
│   │     ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░ 45%               │   │
│   ├───────────────────────────────────────────────┤   │
│   │  2️⃣ 冰霜射手 ❄️                              │   │
│   │     伤害: 620 (33%)  击杀: 1  技能: 6次      │   │
│   │     ▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░ 33%               │   │
│   ├───────────────────────────────────────────────┤   │
│   │  3️⃣ 圣光治疗 ✨                              │   │
│   │     伤害: 420 (22%)  治疗: 560  技能: 5次    │   │
│   │     ▓▓▓▓▓▓░░░░░░░░░░░░░ 22%                 │   │
│   └───────────────────────────────────────────────┘   │
│                                                         │
│   ┌───────────────────────────────────────────────┐   │
│   │  【获得奖励】                                  │   │
│   │     🎫 招募券 x1    ⭐ 经验值 +50             │   │
│   └───────────────────────────────────────────────┘   │
│                                                         │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│   │ 返回主页 │  │ 继续挑战 │  │ 查看详情 │         │
│   └──────────┘  └──────────┘  └──────────┘         │
└─────────────────────────────────────────────────────────┘
```

#### 2.2.2 MVP区域设计

**位置**：顶部突出显示，紧接标题下方

**视觉元素**：
```
┌───────────────────────────────────────────┐
│   ⭐⭐⭐ MVP - 本场最佳 ⭐⭐⭐              │
│                                           │
│       [角色头像]                          │
│         👑                                │
│                                           │
│     角色名称：烈焰战士 🔥                 │
│     总伤害：850  占比：45%                │
│     击杀：2个敌人  技能释放：8次          │
│                                           │
└───────────────────────────────────────────┘
```

**视觉效果**：
- 金色渐变背景（`#FFD700` → `#FFA500`）
- 闪烁光晕特效
- 王冠图标 👑
- 角色名称加粗放大

#### 2.2.3 角色战绩列表设计

**单个角色卡片布局**：
```
┌─────────────────────────────────────────────────┐
│  排名  角色名称+元素  标记                       │
│  ──────────────────────────────────────────── │
│  数据行1：伤害：850 (45%)  击杀：2  技能：8次    │
│  数据行2：伤害进度条 ▓▓▓▓▓░░░░ 45%             │
└─────────────────────────────────────────────────┘
```

**排序规则**：按伤害从高到低排序

**视觉差异**：
- MVP：金色边框 + 王冠图标
- 第2名：银色边框
- 第3名：铜色边框
- 治疗角色：额外显示治疗量

#### 2.2.4 数据进度条设计

**进度条样式**：
```
伤害占比：▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░ 45%
```

**颜色方案**：
- MVP（第1名）：金色 `#FFD700`
- 第2名：蓝色 `#4488ff`
- 第3名：绿色 `#44ff88`
- 治疗角色：绿色 `#00ff88`（特殊标识）

**长度计算**：
```javascript
进度条长度 = (角色伤害 / 总伤害) × 20格
```

---

### 2.3 数据展示规范

#### 2.3.1 数值格式

| 数据类型 | 显示格式 | 示例 |
|---------|---------|------|
| 伤害值 | 整数，千位分隔符 | 1,250 |
| 百分比 | 整数+% | 45% |
| 击杀数 | 整数+单位 | 2个 |
| 技能次数 | 整数+单位 | 8次 |
| 治疗量 | 整数（仅治疗角色） | 560 |

#### 2.3.2 特殊角色标识

**治疗角色特殊显示**：
```
3️⃣ 圣光治疗 ✨
   伤害: 420 (22%)  💚治疗: 560  技能: 5次
   ▓▓▓▓▓▓░░░░░░░░░░░░░ 22%
   [治疗量单独用绿色心形图标标识]
```

**坦克角色特殊显示**：
```
2️⃣ 大地守护 🪨
   伤害: 350 (20%)  🛡️承伤: 1200  技能: 6次
   ▓▓▓▓░░░░░░░░░░░░░░ 20%
   [承受伤害用盾牌图标标识]
```

---

## 三、失败结算界面设计

### 3.1 核心功能设计

#### 3.1.1 数据统计项

**伤害来源分析**（必须记录）：
- 伤害来源类型（敌方角色 / 环境伤害）
- 来源名称（如"幼年爬行者甲"、"火焰燃烧"）
- 造成伤害值
- 伤害占比（百分比）
- 击杀了哪些角色

**环境伤害类型**：
- 火焰燃烧
- 岩浆地块
- Boss技能（炼狱波动、熔岩柱等）

#### 3.1.2 死因分析优先级

**信息层级**：
```
第一层：失败标题 + 基础信息（存活时长、死亡原因）
第二层：伤害来源分析（核心数据）
第三层：战术建议
第四层：操作按钮
```

---

### 3.2 UI布局设计（失败界面）

#### 3.2.1 整体布局结构

```
┌─────────────────────────────────────────────────────────┐
│                     ❌ 战败                              │
│              存活时长：32秒 | 剩余敌人：2个              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌─────────────────────────────────────────────┐     │
│   │   ⚠️ 主要死因：火焰燃烧（环境伤害）          │     │
│   │     建议：招募水系/冰系角色抵抗火焰           │     │
│   └─────────────────────────────────────────────┘     │
│                                                         │
│   ┌───────────────────────────────────────────────┐   │
│   │  【伤害来源分析】                              │   │
│   ├───────────────────────────────────────────────┤   │
│   │  1️⃣ 火焰燃烧 🔥（环境）                       │   │
│   │     造成伤害: 420 (35%)                       │   │
│   │     击杀: 烈焰战士、冰霜射手                  │   │
│   │     ▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░ 35%                │   │
│   ├───────────────────────────────────────────────┤   │
│   │  2️⃣ 火焰祭司 🔥（敌方）                       │   │
│   │     造成伤害: 350 (29%)                       │   │
│   │     击杀: 圣光治疗                            │   │
│   │     ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░ 29%                │   │
│   ├───────────────────────────────────────────────┤   │
│   │  3️⃣ 幼年爬行者·甲 🔥（敌方）                 │   │
│   │     造成伤害: 280 (23%)                       │   │
│   │     击杀: -                                   │   │
│   │     ▓▓▓▓▓▓▓▓▓░░░░░░░░░░ 23%                  │   │
│   ├───────────────────────────────────────────────┤   │
│   │  4️⃣ 烈焰冲锋者·乙 🔥（敌方）                 │   │
│   │     造成伤害: 155 (13%)                       │   │
│   │     击杀: -                                   │   │
│   │     ▓▓▓▓▓░░░░░░░░░░░░░░ 13%                  │   │
│   └───────────────────────────────────────────────┘   │
│                                                         │
│   ┌───────────────────────────────────────────────┐   │
│   │  💡 战术建议                                   │   │
│   │  • 环境伤害占比过高，建议增加火系角色        │   │
│   │  • 优先击杀输出最高的火焰祭司                │   │
│   │  • 考虑带一个治疗角色提升续航               │   │
│   └───────────────────────────────────────────────┘   │
│                                                         │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│   │ 返回主页 │  │ 重新挑战 │  │ 调整阵容 │         │
│   └──────────┘  └──────────┘  └──────────┘         │
└─────────────────────────────────────────────────────────┘
```

#### 3.2.2 主要死因区域设计

**位置**：顶部突出显示，紧接标题下方

**视觉元素**：
```
┌───────────────────────────────────────────┐
│   ⚠️ 主要死因分析                          │
│                                           │
│   最高伤害来源：火焰燃烧（环境伤害）      │
│   累计伤害：420  占比：35%                │
│   击杀角色：烈焰战士、冰霜射手            │
│                                           │
│   💡 战术建议：                            │
│   • 招募火系角色免疫燃烧伤害              │
│   • 快速战斗减少环境伤害累积              │
│   • 考虑使用水系角色克制火系敌人          │
└───────────────────────────────────────────┘
```

**颜色方案**：
- 背景：红色渐变警告色（`#ff4444` → `#cc0000`）
- 文字：白色
- 建议文字：黄色（`#ffdd44`）

#### 3.2.3 伤害来源列表设计

**单个来源卡片布局**：
```
┌─────────────────────────────────────────────────┐
│  排名  来源名称+类型标识                         │
│  ──────────────────────────────────────────── │
│  造成伤害：420 (35%)                            │
│  击杀角色：烈焰战士、冰霜射手                   │
│  伤害进度条 ▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░ 35%          │
└─────────────────────────────────────────────────┘
```

**来源类型标识**：
- 🔥 环境 - 火焰燃烧、岩浆地块
- ⚔️ 敌方 - 具体敌人名称
- 💀 Boss技能 - 特殊技能伤害

#### 3.2.4 战术建议生成规则

**规则表**：

| 场景 | 建议内容 |
|------|---------|
| 环境伤害占比 > 30% | "环境伤害过高，建议增加火系角色免疫燃烧" |
| 某个敌人伤害占比 > 40% | "优先击杀输出最高的[敌人名称]" |
| 全员阵亡 | "考虑带一个治疗角色提升续航能力" |
| 存活时长 < 20秒 | "快速团灭，建议提升角色战力或调整阵容" |
| 远程敌人伤害高 | "敌方远程输出强，考虑使用刺客突进" |

**生成逻辑**：
```
1. 分析伤害来源占比最高的前3项
2. 根据场景匹配对应建议
3. 最多显示3条建议
4. 建议按重要性排序
```

---

### 3.3 数据展示规范（失败界面）

#### 3.3.1 数值格式

| 数据类型 | 显示格式 | 示例 |
|---------|---------|------|
| 伤害值 | 整数，千位分隔符 | 1,250 |
| 百分比 | 整数+% | 35% |
| 击杀角色 | 角色名称列表 | 烈焰战士、冰霜射手 |
| 存活时长 | 秒数 | 32秒 |

#### 3.3.2 来源类型标识

**环境伤害**：
```
1️⃣ 火焰燃烧 🔥（环境）
   造成伤害: 420 (35%)
   影响范围: 全场持续
   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░ 35%
```

**敌方角色伤害**：
```
2️⃣ 火焰祭司 🔥（敌方）
   造成伤害: 350 (29%)
   击杀: 圣光治疗
   ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░ 29%
```

**Boss技能伤害**：
```
3️⃣ 炼狱波动 💀（Boss技能）
   造成伤害: 280 (23%)
   释放者: 火山领主·伊格尼斯
   ▓▓▓▓▓▓▓▓▓░░░░░░░░░░ 23%
```

---

## 四、数据收集与计算逻辑

### 4.1 战斗中需要记录的数据

#### 4.1.1 胜利场景数据

**角色输出统计**（每个玩家角色）：
```typescript
{
  characterId: string,
  characterName: string,
  element: ElementType,
  role: RoleType,
  
  // 核心统计
  totalDamageDealt: number,      // 总伤害输出
  killCount: number,              // 击杀数
  skillUsedCount: number,         // 技能释放次数
  surviveTime: number,            // 存活时长（秒）
  
  // 治疗角色额外统计
  totalHealDone?: number,         // 总治疗量
  healCount?: number,             // 治疗次数
  
  // 坦克角色额外统计
  totalDamageTaken?: number,      // 总承受伤害
  shieldAbsorbed?: number,        // 护盾吸收量
}
```

**MVP计算**：
```javascript
function calculateMVP(characterStats) {
  // 按总伤害排序
  const sorted = characterStats.sort((a, b) => b.totalDamageDealt - a.totalDamageDealt);
  
  // 第一名为MVP
  const mvp = sorted[0];
  
  // 如果伤害相同，使用次要指标
  if (sorted.length > 1 && sorted[0].totalDamageDealt === sorted[1].totalDamageDealt) {
    // 比较击杀数
    if (sorted[0].killCount > sorted[1].killCount) {
      return sorted[0];
    } else if (sorted[0].killCount < sorted[1].killCount) {
      return sorted[1];
    }
    // 比较技能释放次数
    if (sorted[0].skillUsedCount > sorted[1].skillUsedCount) {
      return sorted[0];
    }
  }
  
  return mvp;
}
```

#### 4.1.2 失败场景数据

**伤害来源统计**（每个伤害来源）：
```typescript
{
  sourceId: string,              // 来源ID
  sourceName: string,            // 来源名称
  sourceType: 'enemy' | 'environment' | 'skill', // 来源类型
  
  // 核心统计
  totalDamageDealt: number,      // 造成总伤害
  killCount: number,             // 击杀数
  killedCharacters: string[],    // 击杀的角色名称列表
  
  // 额外信息
  element?: ElementType,         // 元素类型（敌方角色）
  skillName?: string,            // 技能名称（技能伤害）
}
```

**数据来源类型**：
```typescript
enum DamageSourceType {
  ENEMY = 'enemy',           // 敌方角色普通攻击
  ENEMY_SKILL = 'skill',     // 敌方技能
  ENVIRONMENT = 'environment', // 环境伤害（燃烧、岩浆）
  BOSS_SKILL = 'boss_skill',   // Boss特殊技能
}
```

---

### 4.2 数据记录时机

#### 4.2.1 实时记录点

**造成伤害时**：
```javascript
// 在 dealDamage 函数中
function dealDamage(target, damage, sourceUnit) {
  // 记录伤害
  if (sourceUnit.team === 'player') {
    // 记录玩家角色输出
    sourceUnit.stats.totalDamageDealt += damage;
  } else {
    // 记录敌方伤害来源
    recordDamageSource(sourceUnit.character.id, sourceUnit.character.name, damage, target);
  }
  
  // 检查是否击杀
  target.currentHp -= damage;
  if (target.currentHp <= 0) {
    if (sourceUnit.team === 'player') {
      sourceUnit.stats.killCount++;
    } else {
      recordKill(sourceUnit.character.id, target.character.name);
    }
  }
}
```

**技能释放时**：
```javascript
function executeSkill(skill, caster) {
  // 记录技能使用
  if (caster.team === 'player') {
    caster.stats.skillUsedCount++;
  }
  
  // 执行技能效果...
}
```

**环境伤害时**：
```javascript
function applyBurnDamage(unit, damage) {
  // 记录环境伤害
  recordDamageSource('env_burn', '火焰燃烧', damage, unit, 'environment');
  
  unit.currentHp -= damage;
}
```

#### 4.2.2 结算时计算

**胜利结算**：
```javascript
function onVictory() {
  const characterStats = collectPlayerStats();
  const mvp = calculateMVP(characterStats);
  const totalDamage = characterStats.reduce((sum, char) => sum + char.totalDamageDealt, 0);
  
  // 计算百分比
  characterStats.forEach(char => {
    char.damagePercent = Math.round((char.totalDamageDealt / totalDamage) * 100);
  });
  
  showVictoryScreen({
    characters: characterStats,
    mvp: mvp,
    battleTime: getBattleTime(),
    remainingHp: getTeamHpPercent(),
    rewards: getRewards()
  });
}
```

**失败结算**：
```javascript
function onDefeat() {
  const damageSources = collectDamageSourceStats();
  const totalDamage = damageSources.reduce((sum, src) => sum + src.totalDamageDealt, 0);
  
  // 计算百分比
  damageSources.forEach(src => {
    src.damagePercent = Math.round((src.totalDamageDealt / totalDamage) * 100);
  });
  
  // 按伤害排序
  damageSources.sort((a, b) => b.totalDamageDealt - a.totalDamageDealt);
  
  // 生成战术建议
  const suggestions = generateSuggestions(damageSources);
  
  showDefeatScreen({
    damageSources: damageSources,
    primaryCause: damageSources[0],
    suggestions: suggestions,
    surviveTime: getBattleTime(),
    remainingEnemies: getAliveEnemyCount()
  });
}
```

---

## 五、视觉设计规范

### 5.1 颜色方案

#### 5.1.1 胜利界面配色

| 元素 | 颜色代码 | 说明 |
|------|---------|------|
| 背景 | `#1a1a2e` → `#0f3460` | 深蓝渐变 |
| 标题文字 | `#00ff88` | 明亮绿色 |
| MVP背景 | `#FFD700` → `#FFA500` | 金色渐变 |
| MVP边框 | `#FFEB3B` | 亮金色 |
| 第一名进度条 | `#FFD700` | 金色 |
| 第二名进度条 | `#4488ff` | 蓝色 |
| 第三名进度条 | `#44ff88` | 绿色 |
| 治疗数据 | `#00ff88` | 亮绿色 |
| 按钮主色 | `#4488ff` | 蓝色 |

#### 5.1.2 失败界面配色

| 元素 | 颜色代码 | 说明 |
|------|---------|------|
| 背景 | `#2a0000` → `#440000` | 深红渐变 |
| 标题文字 | `#ff4444` | 警告红 |
| 死因背景 | `#ff4444` → `#cc0000` | 红色渐变 |
| 死因边框 | `#ff6666` | 亮红色 |
| 环境伤害色 | `#ff8800` | 橙红色 |
| 敌方伤害色 | `#ff4444` | 红色 |
| 建议文字 | `#ffdd44` | 黄色 |
| 按钮主色 | `#ff6600` | 橙色 |

---

### 5.2 字体规范

| 元素 | 字号 | 字重 | 颜色 |
|------|------|------|------|
| 主标题（胜利/失败） | 48px | Bold | 根据界面类型 |
| 副标题（MVP/死因） | 32px | Bold | 强调色 |
| 角色名称 | 24px | Bold | 白色 |
| 数据数值 | 20px | Regular | 浅灰色 |
| 百分比 | 18px | Bold | 强调色 |
| 建议文字 | 16px | Regular | 黄色 |
| 按钮文字 | 18px | Bold | 白色 |

---

### 5.3 动画效果

#### 5.3.1 入场动画

**胜利界面**：
```
1. 标题从上方飞入（0.3s）
2. MVP区域放大淡入（0.5s，延迟0.2s）
3. 角色卡片依次从左侧滑入（每个0.3s，间隔0.1s）
4. 进度条填充动画（0.8s，延迟0.5s）
5. 按钮淡入（0.3s，延迟1.0s）
```

**失败界面**：
```
1. 标题震动淡入（0.4s）
2. 死因区域闪烁警告（0.6s，延迟0.2s）
3. 伤害来源列表依次显示（每个0.3s，间隔0.1s）
4. 建议文字逐行淡入（0.5s，延迟0.8s）
5. 按钮淡入（0.3s，延迟1.0s）
```

#### 5.3.2 特殊效果

**MVP特效**：
- 金色光晕持续闪烁（2s循环）
- 王冠图标缓慢旋转
- 背景有金色粒子飘落

**死因警告特效**：
- 红色边框闪烁（1s循环，3次）
- 警告图标抖动
- 背景有暗红色脉冲

---

## 六、交互设计

### 6.1 按钮交互

#### 6.1.1 胜利界面按钮

| 按钮 | 功能 | 跳转目标 | 快捷键 |
|------|------|---------|--------|
| 返回主页 | 返回主菜单 | HomePage | ESC |
| 继续挑战 | 重新挑战同一关卡 | BattleScene | Enter |
| 查看详情 | 查看详细数据（扩展功能） | DetailPage | D |

#### 6.1.2 失败界面按钮

| 按钮 | 功能 | 跳转目标 | 快捷键 |
|------|------|---------|--------|
| 返回主页 | 返回主菜单 | HomePage | ESC |
| 重新挑战 | 重新挑战同一关卡 | BattleScene | Enter |
| 调整阵容 | 前往阵容界面 | FormationPage | F |

### 6.2 悬停交互

**角色卡片悬停**：
- 边框高亮
- 显示完整技能列表（tooltip）
- 放大效果（scale: 1.05）

**伤害来源卡片悬停**：
- 边框高亮
- 显示详细伤害明细
- 显示击杀时间线

---

## 七、扩展功能（可选）

### 7.1 详细数据页面

**数据维度**：
- 伤害构成（普攻 vs 技能）
- 技能释放时间轴
- 位置热力图
- 元素克制统计

### 7.2 战斗回放

**功能**：
- 记录关键时刻（击杀、技能、死亡）
- 慢动作回放
- 自由视角

### 7.3 成就系统联动

**触发条件**：
- MVP达成X次
- 单角色伤害占比 > 60%
- 零伤通关
- 快速通关（< 30秒）

---

## 八、技术实现要点

### 8.1 数据结构设计

```typescript
// 战斗统计数据
interface BattleStats {
  // 基础信息
  battleResult: 'victory' | 'defeat';
  battleTime: number;
  levelId: number;
  
  // 胜利数据
  playerStats?: {
    characters: CharacterStat[];
    mvp: CharacterStat;
    totalDamage: number;
    teamHpPercent: number;
  };
  
  // 失败数据
  defeatStats?: {
    damageSources: DamageSource[];
    primaryCause: DamageSource;
    suggestions: string[];
    surviveTime: number;
    remainingEnemies: number;
  };
  
  // 奖励
  rewards?: {
    recruitTickets: number;
    exp: number;
    gold?: number;
  };
}

// 角色统计
interface CharacterStat {
  characterId: string;
  characterName: string;
  element: ElementType;
  role: RoleType;
  totalDamageDealt: number;
  damagePercent: number;
  killCount: number;
  skillUsedCount: number;
  surviveTime: number;
  totalHealDone?: number;
  totalDamageTaken?: number;
}

// 伤害来源
interface DamageSource {
  sourceId: string;
  sourceName: string;
  sourceType: 'enemy' | 'environment' | 'skill' | 'boss_skill';
  totalDamageDealt: number;
  damagePercent: number;
  killCount: number;
  killedCharacters: string[];
  element?: ElementType;
  skillName?: string;
}
```

### 8.2 关键函数

**数据收集**：
```javascript
class BattleStatsCollector {
  private characterStats: Map<string, CharacterStat>;
  private damageSources: Map<string, DamageSource>;
  
  recordDamage(source, target, damage) { /* ... */ }
  recordKill(source, target) { /* ... */ }
  recordSkillUse(caster, skillId) { /* ... */ }
  recordHeal(healer, target, amount) { /* ... */ }
  
  getVictoryStats() { /* ... */ }
  getDefeatStats() { /* ... */ }
}
```

---

## 九、测试验证

### 9.1 测试场景

#### 9.1.1 胜利场景测试

| 测试项 | 预期结果 |
|--------|---------|
| MVP正确性 | 伤害最高的角色获得MVP |
| 数据准确性 | 所有百分比总和为100% |
| 特殊角色显示 | 治疗角色显示治疗量 |
| 排序正确性 | 按伤害从高到低排序 |
| 动画流畅性 | 所有动画按顺序播放 |

#### 9.1.2 失败场景测试

| 测试项 | 预期结果 |
|--------|---------|
| 伤害来源准确 | 记录所有伤害来源 |
| 环境伤害显示 | 正确标识环境伤害 |
| 建议生成 | 生成合理的战术建议 |
| 击杀记录 | 正确记录击杀的角色 |
| 数据完整性 | 所有百分比总和为100% |

### 9.2 边界情况

| 场景 | 处理方式 |
|------|---------|
| 所有角色伤害相同 | 按击杀数、技能次数排序 |
| 纯治疗角色无输出 | 显示治疗量，排在最后 |
| 环境伤害为0 | 不显示环境伤害项 |
| 秒杀（< 5秒） | 标记"快速胜利" |
| 长时间战斗（> 120秒） | 标记"持久战" |

---

## 十、总结

### 10.1 核心价值

- **胜利界面**：突出个人贡献，增强成就感，提供明确的数据反馈
- **失败界面**：分析死因，提供改进建议，引导玩家优化策略

### 10.2 实现优先级

**Phase 1（必须实现）**：
- ✅ 胜利界面：角色伤害统计 + MVP
- ✅ 失败界面：伤害来源分析 + 死因
- ✅ 基础UI布局和动画

**Phase 2（优化）**：
- 战术建议生成
- 特殊角色数据（治疗量、承伤量）
- 详细数据页面

**Phase 3（扩展）**：
- 战斗回放
- 成就系统联动
- 数据分析图表

---

**文档结束**

> 本设计文档提供了完整的战斗结算界面优化方案，包括UI布局、数据统计、交互设计等所有细节。开发团队可根据本文档进行实现，预计实现时间：Phase 1 需要2-3天，Phase 2 需要1-2天。

