# 📞 立即部署 - 跟我做（2分钟）

> 我会一步步告诉你怎么做，非常简单！

---

## 🚀 开始部署

### 第1步：运行部署助手（5秒）

打开命令行，输入：
```bash
cd astrocade
⚡2分钟部署-开始游戏.bat
```

**脚本会自动打开网页和文件，你只需要跟着做：**

---

### 第2步：创建 Supabase 项目（1分钟）

脚本会自动打开 https://supabase.com

**你要做的**：
1. 点击右上角 **"Sign In"**
2. 选择 **"Continue with GitHub"**
3. 点击 **"New Project"**
4. 填写3个信息：
   - **Name**: `astrocade-ladder`
   - **Database Password**: `Astrocade2025!`
   - **Region**: `Tokyo`
5. 点击 **"Create new project"**

⏱️ 等待约1分钟，项目会自动创建

---

### 第3步：导入数据库（30秒）

项目创建完成后：

**你要做的**：
1. 点击左侧菜单 **"SQL Editor"**
2. 点击 **"New query"**
3. 脚本已经打开了 `supabase-schema.sql` 文件
4. **复制全部内容** (Ctrl+A, Ctrl+C)
5. **粘贴到** SQL Editor (Ctrl+V)
6. 点击右下角绿色的 **"Run"** 按钮
7. 看到 **"Success. No rows returned"** ✅

---

### 第4步：获取密钥（10秒）

**你要做的**：
1. 点击左侧 **"Settings"** 齿轮图标
2. 点击 **"API"**
3. 找到两个值：

**第一个值** - `Project URL`:
```
https://xxxxxxxxxxxxx.supabase.co
```
复制它！

**第二个值** - `anon public` (在下面一点):
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJz...（很长）
```
复制它！

---

### 第5步：粘贴到脚本（10秒）

回到命令行窗口，脚本会问你：

```
Project URL (https://xxxxx.supabase.co): 
```
**粘贴第一个值**，按回车

```
anon public key (eyJhbGci...): 
```
**粘贴第二个值**，按回车

✅ 配置完成！

---

### 第6步：自动构建（30秒）

脚本会自动：
- ✅ 安装依赖
- ✅ 构建项目
- ✅ 生成 `dist/` 文件夹

**你什么都不用做，等待即可** ☕

---

## 🎉 完成！

构建完成后，你会看到：

```
✅ 构建完成！文件位于：dist/

下一步：上传到服务器
```

---

## 📤 上传到服务器（2种方式）

### 方式1：本地测试（推荐先测试）

```bash
npx serve dist
```

然后访问 http://localhost:3000

✅ 看到游戏界面
✅ 进入"擂台竞技"
✅ 看到"🟢 在线服务已连接"

**如果测试成功，再上传到服务器！**

---

### 方式2：上传到服务器

**使用 FTP 工具**（最简单）：
1. 下载 FileZilla: https://filezilla-project.org/
2. 连接到服务器：
   - 主机：`43.173.170.5`
   - 端口：`22`
   - 用户名：`root`（或你的用户名）
   - 密码：[你的服务器密码]
3. 上传 `dist/` 文件夹的**所有内容**到服务器的网站目录

**或使用命令行**：
```bash
scp -r dist/* root@43.173.170.5:/var/www/html/
```

---

## 🎮 告诉朋友来玩！

部署完成后，告诉朋友访问：

```
http://43.173.170.5:8080/
```

**每个人**：
1. 进入"擂台竞技"
2. 输入自己的昵称
3. 开始挑战！

**排名会实时同步！** 大家可以看到彼此的排名！⚔️

---

## 🆘 需要帮助？

### 如果卡在某一步

**创建项目时等太久**：
- 正常，Supabase 初始化需要1-2分钟
- 看到进度条就是在创建

**SQL 执行失败**：
- 检查是否复制了完整的 SQL 内容
- 重新复制并执行

**配置后仍显示"未配置"**：
- 检查 `.env.local` 文件是否存在
- 重启开发服务器

**构建失败**：
- 运行 `npm install` 再试一次
- 删除 `node_modules` 和 `package-lock.json`，重新安装

---

## 🎊 成功标志

✅ 本地测试时看到"在线服务已连接"  
✅ 输入昵称后能注册  
✅ 能看到排行榜  
✅ 能挑战对手  
✅ 朋友也能访问并看到你的排名

---

**总耗时：约2分钟**  
**现在就开始吧！** 🚀

