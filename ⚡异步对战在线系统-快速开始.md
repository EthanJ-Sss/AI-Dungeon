# ⚡ 异步对战在线系统 - 快速开始

> 5分钟让玩家通过昵称参与全球排名！  
> 创建日期：2025年10月24日

---

## 🎯 功能简介

✅ **玩家输入昵称即可参与全球排名**  
✅ **实时同步排行榜数据（前30名）**  
✅ **无限挑战，争夺榜首荣誉**  
✅ **支持本地模式和在线模式自动切换**  
✅ **零成本起步（Supabase 免费版）**

---

## 🚀 立即开始

### 选项1：本地模式（无需配置，开箱即用）

```bash
cd astrocade
npm run dev
```

✅ 自动使用 mock 数据  
✅ 无需注册，立即可玩  
✅ 适合本地测试

---

### 选项2：在线模式（5分钟配置）

#### 第1步：创建 Supabase 项目（2分钟）

1. 访问 https://supabase.com 并登录
2. 点击 "New Project"
3. 填写：
   - 项目名称：`astrocade-ladder`
   - 密码：设置一个强密码
   - 区域：Tokyo（中国用户推荐）
4. 等待初始化完成

#### 第2步：执行数据库脚本（1分钟）

1. 在 Supabase Dashboard，点击 "SQL Editor"
2. 打开项目文件：`astrocade/supabase-schema.sql`
3. 复制全部内容，粘贴到 SQL Editor
4. 点击 "Run" 执行

#### 第3步：配置环境变量（1分钟）

1. 在 Supabase Dashboard，进入 "Settings" → "API"
2. 复制 `Project URL` 和 `anon public key`
3. 在项目目录创建 `.env.local` 文件：

```bash
# astrocade/.env.local
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### 第4步：启动项目（1分钟）

```bash
cd astrocade
npm run dev
```

✅ 浏览器访问 http://localhost:5173  
✅ 进入"擂台竞技"  
✅ 看到"在线服务已连接"绿色提示  
✅ 输入昵称注册，开始挑战！

---

## 📁 核心文件说明

| 文件 | 说明 | 状态 |
|------|------|-----|
| `supabase-schema.sql` | 数据库表结构 | ✅ 已创建 |
| `src/services/supabase.ts` | Supabase 配置 | ✅ 已创建 |
| `src/services/ladderApi.ts` | API 服务层 | ✅ 已创建 |
| `src/store/ladderStoreOnline.ts` | 在线模式 Store | ✅ 已创建 |
| `src/components/PlayerRegisterModal.tsx` | 注册界面 | ✅ 已创建 |
| `.env.local` | 环境变量 | ⚠️ 需手动创建 |

---

## 🔧 如何集成到现有代码

### 当前代码（本地模式）
```typescript
import { useLadderStore } from '../store/ladderStore';

// 同步操作
const { initializeLadder } = useLadderStore();
initializeLadder();
```

### 升级为在线模式
```typescript
import { useLadderStoreOnline } from '../store/ladderStoreOnline';
import PlayerRegisterModal from './PlayerRegisterModal';

// 异步操作
const { 
  initializeLadder, 
  registerPlayer,
  playerRegistered 
} = useLadderStoreOnline();

useEffect(() => {
  initializeLadder(); // 自动识别在线/本地模式
}, []);

// 显示注册界面（首次使用）
{!playerRegistered && (
  <PlayerRegisterModal
    onRegisterSuccess={async (playerName) => {
      await registerPlayer(playerName);
    }}
  />
)}
```

**关键变化**：
- ✅ 函数改为异步（加 `await`）
- ✅ 新增注册界面
- ✅ 自动识别在线/本地模式

---

## 🎮 测试流程

### 1. 本地测试
```bash
npm run dev
```
- ✅ 应该看到 mock 数据
- ✅ 可以挑战、更新排名
- ✅ 数据保存在 localStorage

### 2. 在线测试
1. 配置 `.env.local`
2. 刷新浏览器
3. 应该看到"在线服务已连接"
4. 输入昵称注册
5. 挑战对手，排名同步到服务器
6. 在 Supabase Dashboard → Table Editor 查看数据

---

## 🐛 常见问题

### Q: 显示"Supabase 未配置"
**A**: 检查 `.env.local` 文件是否存在，变量名是否正确（`VITE_` 前缀）

### Q: 注册后无反应
**A**: 
1. 检查浏览器控制台是否有错误
2. 检查 Supabase 项目是否正常运行
3. 确认 Project URL 和 anon key 正确

### Q: 如何重置数据？
**A**: 在 Supabase SQL Editor 执行：
```sql
DELETE FROM challenge_records;
DELETE FROM ladder_players;
```

---

## 📊 系统架构

```
┌─────────────────────────────────────┐
│         React 前端                   │
│  - 昵称注册界面                      │
│  - 排行榜显示                        │
│  - 挑战流程                          │
└──────────────┬──────────────────────┘
               │
               │ HTTPS/REST API
               ↓
┌─────────────────────────────────────┐
│      Supabase 后端服务               │
│  - PostgreSQL 数据库                │
│  - 实时订阅 (可选)                  │
│  - 行级安全策略                      │
└─────────────────────────────────────┘
```

---

## 🎯 下一步

### MVP 阶段（当前）
- [x] 昵称注册
- [x] 排行榜同步
- [x] 挑战数据记录
- [x] 本地/在线自动切换

### v1.1 - 增强功能
- [ ] 用户认证（邮箱/密码）
- [ ] 战斗结果验证（防作弊）
- [ ] 实时排名推送

### v1.2 - 社交功能
- [ ] 好友系统
- [ ] 战报分享
- [ ] 观战回放

---

## 📚 详细文档

- [✅ 部署指南](./✅异步对战在线系统部署指南.md) - 完整部署步骤
- [🎮 设计文档](./联网异步对战系统设计文档.md) - 系统设计细节
- [🔧 集成说明](./astrocade/在线系统集成说明.md) - 代码集成指南

---

## 🎉 完成状态

- [x] Supabase 客户端配置
- [x] 数据库表结构设计
- [x] API 服务层实现
- [x] 昵称注册界面
- [x] 在线模式 Store
- [x] 实时排名同步
- [x] 部署指南文档
- [x] 快速开始指南

**所有功能就绪，可以立即使用！** 🚀

---

## 💡 温馨提示

1. **MVP 阶段安全性**
   - 当前适合小规模测试（<100人）
   - 适合朋友间游玩
   - 正式运营前需添加用户认证和反作弊

2. **免费额度**
   - Supabase 免费版：500MB 数据库，50GB 带宽/月
   - 预计支持 1000+ 玩家

3. **性能优化**
   - 排行榜固定30人，查询速度极快
   - 已创建数据库索引
   - 无性能瓶颈

---

**开始你的在线擂台之旅吧！** ⚔️✨

**需要帮助？** 查看详细文档或访问 https://supabase.com/docs

---

**创建时间**：2025年10月24日  
**版本**：v1.0 (MVP)  
**开发者**：Cursor AI Assistant

