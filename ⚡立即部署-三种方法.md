# ⚡ 立即部署 - 选择一种方法

## 🎯 三种部署方法

根据你的系统环境，选择最适合的方法：

---

## 方法1: 一键部署（最简单）⭐

**适用于**: Windows用户，有SSH客户端

### 步骤：

1. **双击运行**: `一键部署到服务器.bat`

2. **输入密码**: 当提示时输入 `MTc1MjA0NDQ0MQ`

3. **等待完成**: 脚本会自动完成所有部署步骤

4. **访问测试**: 打开浏览器访问 http://43.173.170.5

### 如果遇到问题：
- 确保安装了OpenSSH（Windows 10/11自带）
- 尝试方法2或方法3

---

## 方法2: Git Bash部署（推荐）⭐⭐

**适用于**: 安装了Git的用户

### 步骤：

1. **打开Git Bash**: 
   - 在项目根目录右键 → "Git Bash Here"

2. **运行部署脚本**:
   ```bash
   chmod +x scripts/deploy/deploy-to-server.sh
   ./scripts/deploy/deploy-to-server.sh
   ```

3. **输入密码**: 
   - 首次连接时输入 `yes` 确认
   - 输入密码: `MTc1MjA0NDQ0MQ`

4. **等待完成**: 约需3-5分钟

5. **访问测试**: http://43.173.170.5

---

## 方法3: 手动部署（最稳定）⭐⭐⭐

**适用于**: 所有用户，特别是遇到自动部署问题时

### 步骤：

#### 1. 连接服务器

**使用PowerShell**:
```powershell
ssh ubuntu@43.173.170.5
# 密码: MTc1MjA0NDQ0MQ
```

**或使用PuTTY**:
- Host: 43.173.170.5
- Port: 22
- Username: ubuntu
- Password: MTc1MjA0NDQ0MQ

#### 2. 复制粘贴以下命令块

连接成功后，复制以下整个命令块，粘贴到终端：

```bash
# ========== 一键部署命令 ==========
set -e

echo "🚀 开始部署..."

# 1. 安装Node.js
if ! command -v node &> /dev/null; then
    echo "📦 安装Node.js..."
    curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
    sudo apt-get install -y nodejs
fi
echo "✅ Node.js: $(node -v)"

# 2. 安装Git
if ! command -v git &> /dev/null; then
    echo "📦 安装Git..."
    sudo apt-get update && sudo apt-get install -y git
fi

# 3. 安装Nginx
if ! command -v nginx &> /dev/null; then
    echo "📦 安装Nginx..."
    sudo apt-get update && sudo apt-get install -y nginx
fi

# 4. 克隆/更新项目
sudo mkdir -p /var/www/ai-dungeon
sudo chown -R ubuntu:ubuntu /var/www/ai-dungeon

if [ -d "/var/www/ai-dungeon/.git" ]; then
    echo "📥 更新项目..."
    cd /var/www/ai-dungeon && git pull origin main
else
    echo "📥 克隆项目..."
    git clone https://github.com/EthanJ-Sss/AI-Dungeon.git /var/www/ai-dungeon
fi

# 5. 安装依赖和构建
echo "📦 安装依赖..."
cd /var/www/ai-dungeon/astrocade
npm install

echo "🏗️ 构建项目..."
npm run build

# 6. 配置Nginx
echo "🌐 配置Nginx..."
sudo tee /etc/nginx/sites-available/ai-dungeon > /dev/null << 'EOF'
server {
    listen 80;
    server_name 43.173.170.5;
    root /var/www/ai-dungeon/astrocade/dist;
    index index.html;
    
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    access_log /var/log/nginx/ai-dungeon-access.log;
    error_log /var/log/nginx/ai-dungeon-error.log;
}
EOF

sudo ln -sf /etc/nginx/sites-available/ai-dungeon /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl restart nginx
sudo systemctl enable nginx

# 7. 配置防火墙
sudo ufw allow 80/tcp 2>&1 || true
sudo ufw allow 443/tcp 2>&1 || true

echo ""
echo "✅ 部署成功！"
echo "🌐 访问地址: http://43.173.170.5"
```

#### 3. 等待完成

命令会自动执行所有部署步骤，约需3-5分钟。

#### 4. 验证部署

打开浏览器访问: http://43.173.170.5

---

## ✅ 部署成功的标志

你应该看到：
- ✅ 游戏主界面正常显示
- ✅ 可以点击"开始游戏"
- ✅ 招募、编队等功能正常

---

## ❗ 部署后必做事项

### 1. 立即修改密码（重要！）

```bash
# 在服务器上运行
ssh ubuntu@43.173.170.5
passwd
# 输入新密码
```

### 2. 配置SSH密钥（推荐）

**在本地PowerShell运行**:
```powershell
# 生成密钥（如果没有）
ssh-keygen -t rsa -b 4096

# 复制公钥到服务器
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh ubuntu@43.173.170.5 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

之后就可以无密码登录了！

---

## 🔄 后续更新

当你在GitHub推送新代码后，在服务器运行：

```bash
ssh ubuntu@43.173.170.5
cd /var/www/ai-dungeon && git pull origin main
cd astrocade && npm install && npm run build
sudo systemctl restart nginx
```

或者保存为脚本方便使用。

---

## 📋 快速参考

| 项目 | 值 |
|------|-----|
| 服务器IP | 43.173.170.5 |
| 用户名 | ubuntu |
| 密码 | MTc1MjA0NDQ0MQ |
| 网站地址 | http://43.173.170.5 |
| 项目目录 | /var/www/ai-dungeon |
| Nginx配置 | /etc/nginx/sites-available/ai-dungeon |
| 访问日志 | /var/log/nginx/ai-dungeon-access.log |

---

## 🆘 遇到问题？

1. **查看详细部署指南**: `🚀部署指南.md`
2. **查看手动部署步骤**: `scripts/deploy/deploy-manual.md`
3. **查看错误日志**:
   ```bash
   ssh ubuntu@43.173.170.5
   sudo tail -100 /var/log/nginx/error.log
   ```

---

## 🎯 推荐部署流程

1. 首先尝试 **方法1（一键部署）**
2. 如果失败，使用 **方法2（Git Bash）**
3. 如果还是不行，使用 **方法3（手动）**

**方法3是最稳定的，适合所有情况！**

---

**现在就开始部署吧！🚀**


