# ✅ AI-Dungeon 8080端口部署完成

## 🎉 部署成功！

**新项目访问地址**: http://43.173.170.5:8080  
**部署时间**: 2025年10月23日 19:32  
**状态**: ✅ 运行正常

---

## 🌐 端口分配

| 端口 | 项目 | 状态 |
|------|------|------|
| **80** | 原有项目 | ✅ 保持运行 |
| **8080** | **AI-Dungeon (新)** | ✅ **正在运行** |

### 访问地址：

- **原有项目**: http://43.173.170.5
- **AI-Dungeon**: http://43.173.170.5:8080 ⭐

---

## 📋 完成的配置

### 1. Apache虚拟主机配置 ✅

配置文件位置: `/etc/apache2/sites-available/ai-dungeon-8080.conf`

```apache
<VirtualHost *:8080>
    ServerName 43.173.170.5
    DocumentRoot /var/www/ai-dungeon/astrocade/dist

    <Directory /var/www/ai-dungeon/astrocade/dist>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        
        # 单页应用路由支持
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>

    ErrorLog /var/log/apache2/ai-dungeon-error.log
    CustomLog /var/log/apache2/ai-dungeon-access.log combined
</VirtualHost>
```

### 2. 端口监听配置 ✅

配置文件: `/etc/apache2/ports.conf`

```apache
Listen 80
Listen 8080

<IfModule ssl_module>
    Listen 443
</IfModule>
```

### 3. Apache模块 ✅

已启用的模块：
- ✅ `rewrite` - URL重写（单页应用支持）
- ✅ `deflate` - Gzip压缩
- ✅ `filter` - 内容过滤

### 4. 防火墙配置 ✅

已开放端口：
- ✅ 22/tcp (SSH)
- ✅ 80/tcp (HTTP - 原项目)
- ✅ **8080/tcp (HTTP - AI-Dungeon)**
- ✅ 443/tcp (HTTPS)

### 5. 服务状态 ✅

```bash
● apache2.service - The Apache HTTP Server
   Loaded: loaded
   Active: active (running)
   
监听端口:
   :::80   (原项目)
   :::8080 (AI-Dungeon) ⭐
```

---

## 📁 项目文件结构

```
服务器: /var/www/ai-dungeon/
├── astrocade/
│   ├── dist/              ⭐ 部署目录 (被8080端口服务)
│   │   ├── index.html
│   │   └── assets/
│   │       ├── index-CWu1qRyj.css
│   │       └── index-ClMmpnsg.js
│   ├── src/
│   ├── node_modules/
│   └── package.json
└── [其他文件]
```

---

## 🔧 部署脚本

创建的部署工具：

1. **deploy_port_simple.py** - 8080端口部署脚本
2. **fix_port_listen.py** - 端口监听修复脚本 ⭐

### 使用方法：

```bash
# 初次部署到8080端口
py -3 deploy_port_simple.py

# 修复端口监听问题
py -3 fix_port_listen.py
```

---

## 🧪 测试验证

### 本地测试（服务器上）：
```bash
curl -I http://localhost:8080
```

**预期结果**:
```
HTTP/1.1 200 OK
Server: Apache/2.4.58 (Ubuntu)
Content-Type: text/html
Content-Length: 456
```

### 外部访问测试：
```bash
curl -I http://43.173.170.5:8080
```

### 浏览器访问：
打开浏览器访问: **http://43.173.170.5:8080**

---

## 📝 后续维护

### 更新AI-Dungeon项目：

```bash
# 1. SSH连接到服务器
ssh ubuntu@43.173.170.5

# 2. 进入项目目录
cd /var/www/ai-dungeon

# 3. 拉取最新代码
git pull origin main

# 4. 重新构建
cd astrocade
npm install --legacy-peer-deps
npm run build

# 5. 无需重启Apache（静态文件已更新）
```

### 查看日志：

```bash
# AI-Dungeon访问日志
sudo tail -f /var/log/apache2/ai-dungeon-access.log

# AI-Dungeon错误日志
sudo tail -f /var/log/apache2/ai-dungeon-error.log

# Apache主日志
sudo tail -f /var/log/apache2/error.log

# 系统日志
sudo journalctl -u apache2 -f
```

### 重启服务：

```bash
# 测试Apache配置
sudo apache2ctl configtest

# 重启Apache
sudo systemctl restart apache2

# 检查状态
sudo systemctl status apache2

# 检查端口监听
sudo netstat -tlnp | grep apache2
# 或
sudo ss -tlnp | grep apache2
```

---

## 🐛 故障排除

### 问题1: 8080端口无法访问

**诊断步骤**：

```bash
# 1. 检查Apache是否在8080端口监听
sudo netstat -tlnp | grep 8080

# 2. 检查防火墙
sudo ufw status

# 3. 检查Apache配置
sudo apache2ctl configtest

# 4. 检查站点是否启用
ls -la /etc/apache2/sites-enabled/ | grep 8080

# 5. 查看错误日志
sudo tail -50 /var/log/apache2/error.log
```

**解决方案**：

```bash
# 重新运行修复脚本
py -3 fix_port_listen.py

# 或手动修复
sudo nano /etc/apache2/ports.conf  # 添加 Listen 8080
sudo a2ensite ai-dungeon-8080.conf
sudo systemctl restart apache2
```

### 问题2: 页面显示404

**可能原因**：
- dist目录不存在或为空
- 文件权限问题

**解决方案**：

```bash
# 检查dist目录
ls -la /var/www/ai-dungeon/astrocade/dist/

# 如果目录为空，重新构建
cd /var/www/ai-dungeon/astrocade
npm run build

# 修复权限
sudo chown -R www-data:www-data /var/www/ai-dungeon/astrocade/dist
sudo chmod -R 755 /var/www/ai-dungeon/astrocade/dist
```

### 问题3: 页面白屏或路由不工作

**可能原因**：
- Rewrite模块未启用
- .htaccess未生效

**解决方案**：

```bash
# 启用rewrite模块
sudo a2enmod rewrite

# 重启Apache
sudo systemctl restart apache2

# 确认AllowOverride设置正确（应为All）
```

### 问题4: 两个端口互相冲突

**症状**：
- 80端口显示AI-Dungeon
- 8080端口显示原项目

**解决方案**：

```bash
# 查看所有启用的站点
ls -la /etc/apache2/sites-enabled/

# 检查每个配置文件的DocumentRoot
grep -r "DocumentRoot" /etc/apache2/sites-enabled/

# 确保配置正确：
# - 80端口 -> 原项目路径
# - 8080端口 -> /var/www/ai-dungeon/astrocade/dist
```

---

## 📊 性能监控

### 检查Apache性能：

```bash
# 查看Apache状态
sudo apache2ctl status

# 查看连接数
sudo netstat -an | grep :8080 | wc -l

# 查看进程
ps aux | grep apache2
```

### 监控资源使用：

```bash
# CPU和内存
htop

# 磁盘使用
df -h

# 网络流量
sudo iftop -i eth0
```

---

## 🔒 安全建议

### 1. 限制访问（可选）

如果8080端口只需特定IP访问：

```apache
<Directory /var/www/ai-dungeon/astrocade/dist>
    Require ip 192.168.1.0/24
    Require ip 你的IP地址
</Directory>
```

### 2. 启用HTTPS

使用Let's Encrypt为8080端口配置SSL：

```bash
sudo apt-get install certbot python3-certbot-apache
sudo certbot --apache -d yourdomain.com
```

### 3. 限制请求大小

在VirtualHost配置中添加：

```apache
LimitRequestBody 10485760  # 10MB
```

### 4. 启用安全头

```apache
Header always set X-Frame-Options "SAMEORIGIN"
Header always set X-Content-Type-Options "nosniff"
Header always set X-XSS-Protection "1; mode=block"
```

---

## 📈 后续优化建议

### 1. 性能优化

- [ ] 启用HTTP/2
- [ ] 配置浏览器缓存
- [ ] 启用Brotli压缩
- [ ] 使用CDN加速静态资源

### 2. 功能扩展

- [ ] 配置域名访问
- [ ] 设置HTTPS
- [ ] 配置自动备份
- [ ] 添加监控告警

### 3. 开发效率

- [ ] 配置CI/CD自动部署
- [ ] 设置Git Webhooks
- [ ] 创建部署脚本快捷方式

---

## ✅ 验证清单

- [x] 8080端口已配置监听
- [x] Apache虚拟主机已创建
- [x] 站点配置已启用
- [x] Rewrite模块已启用
- [x] 防火墙已开放8080端口
- [x] Apache服务正常运行
- [x] 本地curl测试成功 (HTTP 200)
- [x] dist目录存在且有文件
- [x] 日志文件可写
- [ ] 外部浏览器访问测试 (待你验证)
- [ ] 游戏功能测试 (待你验证)

---

## 🎯 下一步

### 立即验证：

1. **打开浏览器访问**: http://43.173.170.5:8080
2. **测试游戏功能**：
   - 招募系统
   - 关卡选择
   - 战斗系统
3. **检查新功能**：
   - 进度解锁系统
   - 新增角色
   - 羁绊系统

### 如果访问成功：

- ✅ 恭喜！双端口部署完成
- 📝 建议配置域名指向
- 🔒 建议配置HTTPS

### 如果遇到问题：

- 📧 查看错误日志
- 🔧 运行故障排除命令
- 💬 参考上面的故障排除章节

---

## 📞 快速参考

| 操作 | 命令 |
|------|------|
| 查看Apache状态 | `sudo systemctl status apache2` |
| 重启Apache | `sudo systemctl restart apache2` |
| 测试配置 | `sudo apache2ctl configtest` |
| 查看8080端口 | `sudo netstat -tlnp \| grep 8080` |
| 查看访问日志 | `sudo tail -f /var/log/apache2/ai-dungeon-access.log` |
| 查看错误日志 | `sudo tail -f /var/log/apache2/ai-dungeon-error.log` |
| 更新代码 | `cd /var/www/ai-dungeon && git pull` |
| 重新构建 | `cd astrocade && npm run build` |

---

## 🎉 总结

**AI-Dungeon已成功部署到8080端口！**

- ✅ 原有80端口项目保持不变
- ✅ 新项目运行在8080端口
- ✅ 两个项目互不干扰
- ✅ 配置完整且可维护

**现在可以访问 http://43.173.170.5:8080 体验AI-Dungeon了！** 🎮

---

**部署完成时间**: 2025-10-23 19:32 (CST)  
**服务器**: 43.173.170.5  
**端口**: 8080  
**状态**: ✅ 正常运行



