# ✅ 立即部署 - 完整指南

## 🎯 当前状态

✅ **所有代码已修复**
✅ **前端已构建完成**
✅ **部署文件已准备就绪**

**部署文件位置**: `E:\circletest\astrocade\dist\`

---

## 🚀 立即部署（3种方法）

### ⚡ 方法1: 使用 WinSCP（最简单 - 推荐）

**下载 WinSCP**: https://winscp.net/

**步骤**:
1. 打开 WinSCP
2. 新建站点：
   - 文件协议: `SFTP`
   - 主机名: `43.173.170.5`
   - 端口: `22`
   - 用户名: `root`
   - 密码: `[你的服务器密码]`
3. 点击"登录"
4. 左侧：浏览到 `E:\circletest\astrocade\dist\`
5. 右侧：进入 `/var/www/html/`
6. 选择 dist 下的所有文件（Ctrl+A）
7. 拖拽到右侧上传
8. ✅ 完成！

---

### ⚡ 方法2: 使用 FileZilla

**下载 FileZilla**: https://filezilla-project.org/

**步骤**:
1. 打开 FileZilla
2. 文件 → 站点管理器 → 新建站点
3. 设置：
   - 协议: `SFTP`
   - 主机: `43.173.170.5`
   - 端口: `22`
   - 登录类型: `正常`
   - 用户: `root`
   - 密码: `[你的服务器密码]`
4. 点击"连接"
5. 左侧：浏览到 `E:\circletest\astrocade\dist\`
6. 右侧：进入 `/var/www/html/`
7. 选择 dist 下的所有文件
8. 右键 → 上传
9. ✅ 完成！

---

### ⚡ 方法3: 使用命令行（需要配置SSH密钥）

**如果你已配置SSH密钥**:

```bash
cd E:\circletest\astrocade
scp -r dist/* root@43.173.170.5:/var/www/html/
```

**如果需要输入密码**:

```bash
cd E:\circletest\astrocade
scp -r dist/* root@43.173.170.5:/var/www/html/
# 然后输入服务器密码
```

---

## 📋 需要上传的文件

**位置**: `E:\circletest\astrocade\dist\`

**包含**:
```
dist/
├── index.html                    (主页面)
├── assets/
│   ├── index-xxx.css            (样式文件)
│   └── index-xxx.js             (JavaScript文件)
└── vite.svg                     (图标)
```

**目标服务器路径**: `/var/www/html/`

---

## 🎮 部署后立即测试

### 步骤1: 访问游戏

打开浏览器访问: `http://43.173.170.5:8080/`

### 步骤2: 注册并设置阵容

1. 点击"擂台竞技"
2. 输入昵称（例如："战神无敌"）
3. 看到提示：**"是否立即设置防守阵容？"**
4. 点击"**确定**"
5. 选择3个角色
6. 点击"**保存阵容**"

### 步骤3: 开始挑战

1. 返回擂台
2. 看到30个预设敌人
3. 点击第30名的"**⚔️ 挑战**"按钮（现在是可用的！）
4. 确认挑战
5. ✅ 开始战斗！

---

## 🔍 验证部署成功

**检查1**: 访问 `http://43.173.170.5:8080/`
- ✅ 应该看到游戏主页

**检查2**: 打开浏览器控制台（F12）
- ✅ 应该没有404错误
- ✅ 应该看到 `[天梯Store]` 相关日志

**检查3**: 进入擂台竞技
- ✅ 看到注册弹窗
- ✅ 可以正常注册
- ✅ 看到30个预设敌人

**检查4**: 设置防守阵容后
- ✅ 所有敌人的"挑战"按钮变为红色可用状态
- ✅ 点击挑战可以正常开始战斗

---

## 🐛 如果遇到问题

### 问题1: 无法连接服务器

**解决**:
- 确认服务器IP正确: `43.173.170.5`
- 确认服务器端口22开放
- 确认用户名和密码正确

### 问题2: 上传失败

**解决**:
- 确认目标目录 `/var/www/html/` 有写入权限
- 尝试先删除目标目录的旧文件
- 使用 `sudo` 权限

### 问题3: 页面显示旧版本

**解决**:
- 清除浏览器缓存（Ctrl+Shift+Delete）
- 硬刷新（Ctrl+F5）
- 或使用无痕模式测试

---

## 📞 后端服务器

**如果需要在线多人模式，还需要启动后端**:

```bash
# SSH登录服务器
ssh root@43.173.170.5

# 进入后端目录（如果已上传）
cd /root/astrocade-server

# 安装依赖（首次）
npm install

# 启动后端
pm2 start index.js --name ladder-api
pm2 save
```

**后端API地址**: `http://43.173.170.5:3001/api/health`

---

## ✅ 完成清单

部署前端：
- [ ] 使用FTP工具连接服务器
- [ ] 上传 `dist/` 目录所有文件到 `/var/www/html/`
- [ ] 访问 `http://43.173.170.5:8080/` 验证
- [ ] 测试注册流程
- [ ] 测试挑战功能

可选 - 启动后端：
- [ ] SSH登录服务器
- [ ] 上传后端代码（如需要）
- [ ] 启动后端服务
- [ ] 验证在线模式

---

## 🎉 准备开始！

**现在只需要**:

1. **下载并打开 WinSCP** (https://winscp.net/)
2. **连接到服务器** (`43.173.170.5`)
3. **上传 dist 文件夹的内容**
4. **访问游戏并测试**

**你和朋友们就可以开始游戏了！** 🎮🔥

---

## 💡 快速提示

**最快部署方法**:
1. 打开 WinSCP
2. 连接 `43.173.170.5`
3. 拖拽上传
4. 完成！

**预计时间**: 2-3分钟

**难度**: ⭐ 非常简单


