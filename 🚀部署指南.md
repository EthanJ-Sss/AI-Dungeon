# 🚀 AI-Dungeon 服务器部署指南

## 📋 概览

本指南将帮助你将AI-Dungeon项目部署到远程服务器。

**服务器信息**:
- IP: 43.173.170.5
- 用户名: ubuntu
- 系统: Ubuntu Linux

---

## ⚡ 快速部署（推荐）

### 方法1: 一键部署脚本

**Windows用户**:

1. 双击运行 `一键部署到服务器.bat`
2. 按照提示输入服务器密码
3. 等待部署完成
4. 浏览器访问: http://43.173.170.5

**提示**: 如果遇到SSH连接问题，请使用方法2

---

### 方法2: 使用Git Bash（更稳定）

1. 右键点击项目根目录，选择"Git Bash Here"
2. 运行以下命令:

```bash
chmod +x scripts/deploy/deploy-to-server.sh
./scripts/deploy/deploy-to-server.sh
```

3. 输入密码: `MTc1MjA0NDQ0MQ`
4. 等待部署完成

---

## 📖 手动部署

如果自动脚本不工作，请参考详细的手动部署指南：

👉 [查看手动部署指南](scripts/deploy/deploy-manual.md)

**主要步骤**:
1. SSH连接到服务器
2. 安装Node.js, Git, Nginx
3. 克隆项目代码
4. 构建项目
5. 配置Nginx
6. 开放防火墙端口

---

## ✅ 验证部署

部署完成后，访问: **http://43.173.170.5**

你应该能看到游戏的主界面。

---

## 🔄 更新项目

当你在GitHub上推送新代码后，在服务器上运行：

```bash
cd /var/www/ai-dungeon
git pull origin main
cd astrocade
npm install
npm run build
sudo systemctl restart nginx
```

或者创建一个更新脚本 `/var/www/update.sh`:

```bash
#!/bin/bash
cd /var/www/ai-dungeon
echo "📥 拉取最新代码..."
git pull origin main
cd astrocade
echo "📦 安装依赖..."
npm install
echo "🏗️ 构建项目..."
npm run build
echo "🔄 重启Nginx..."
sudo systemctl restart nginx
echo "✅ 更新完成！"
```

使用方法:
```bash
chmod +x /var/www/update.sh
/var/www/update.sh
```

---

## 🔒 安全加固（重要！）

### 1. 立即修改密码

```bash
# SSH连接到服务器
ssh ubuntu@43.173.170.5

# 修改密码
passwd
```

### 2. 配置SSH密钥认证

**在本地电脑上**:

```powershell
# 生成SSH密钥（如果没有）
ssh-keygen -t rsa -b 4096

# 查看公钥
type $env:USERPROFILE\.ssh\id_rsa.pub
```

**复制公钥，然后在服务器上**:

```bash
# 添加公钥
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
# 粘贴公钥，保存退出

# 设置权限
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### 3. 禁用密码登录（可选）

确保SSH密钥能正常工作后：

```bash
sudo nano /etc/ssh/sshd_config
# 找到并修改：PasswordAuthentication no
sudo systemctl restart sshd
```

---

## 🌐 配置域名（可选）

如果你有域名，可以这样配置：

### 1. DNS设置

在域名管理面板添加A记录：
- 类型: A
- 主机: @（或www）
- 值: 43.173.170.5

### 2. 修改Nginx配置

```bash
sudo nano /etc/nginx/sites-available/ai-dungeon
```

修改 `server_name`:
```nginx
server_name yourdomain.com www.yourdomain.com;
```

### 3. 配置HTTPS（免费）

```bash
# 安装Certbot
sudo apt-get install -y certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# 测试自动续期
sudo certbot renew --dry-run
```

---

## 📊 监控和日志

### 查看访问日志
```bash
sudo tail -f /var/log/nginx/ai-dungeon-access.log
```

### 查看错误日志
```bash
sudo tail -f /var/log/nginx/ai-dungeon-error.log
```

### 查看Nginx状态
```bash
sudo systemctl status nginx
```

### 查看服务器资源
```bash
# CPU和内存
htop

# 磁盘使用
df -h

# 网络连接
sudo netstat -tulpn
```

---

## ❌ 常见问题

### 问题1: 无法连接SSH

**解决方法**:
- 检查服务器IP是否正确
- 检查网络连接
- 检查防火墙是否允许SSH（端口22）

### 问题2: 构建失败

**解决方法**:
```bash
# 清除缓存重试
cd /var/www/ai-dungeon/astrocade
rm -rf node_modules package-lock.json
npm install
npm run build
```

### 问题3: 页面显示空白

**解决方法**:
```bash
# 检查dist目录是否存在
ls -la /var/www/ai-dungeon/astrocade/dist/

# 检查Nginx配置
sudo nginx -t

# 查看错误日志
sudo tail -100 /var/log/nginx/error.log
```

### 问题4: 80端口被占用

**解决方法**:
```bash
# 查看占用端口的进程
sudo netstat -tulpn | grep :80

# 如果是Apache
sudo systemctl stop apache2
sudo systemctl disable apache2
```

---

## 🛠️ 高级配置

### 启用HTTP/2

编辑Nginx配置:
```nginx
listen 443 ssl http2;
```

### 配置缓存

```nginx
# 在server块内添加
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### 启用Brotli压缩

```bash
# 安装Brotli模块
sudo apt-get install -y nginx-module-brotli

# 在Nginx配置中启用
brotli on;
brotli_types text/plain text/css application/json application/javascript;
```

---

## 📞 获取帮助

- **手动部署指南**: `scripts/deploy/deploy-manual.md`
- **GitHub仓库**: https://github.com/EthanJ-Sss/AI-Dungeon
- **Nginx文档**: https://nginx.org/en/docs/
- **Let's Encrypt**: https://letsencrypt.org/

---

## ✨ 部署检查清单

- [ ] 项目已推送到GitHub
- [ ] 服务器环境已准备（Node.js, Git, Nginx）
- [ ] 项目代码已克隆到服务器
- [ ] 依赖已安装
- [ ] 项目已构建
- [ ] Nginx已配置并重启
- [ ] 防火墙端口已开放
- [ ] 网站可以正常访问
- [ ] 服务器密码已修改
- [ ] SSH密钥认证已配置
- [ ] （可选）域名已配置
- [ ] （可选）HTTPS已启用

---

**祝部署顺利！🎉**

部署成功后，记得：
1. ⚠️ **立即修改服务器密码**
2. 🔐 配置SSH密钥认证
3. 🌐 考虑配置域名和HTTPS
4. 📊 定期查看日志和监控


