# 🚀 AI-Dungeon 部署方案

## 当前状态
- ✅ 项目已上传到GitHub
- ✅ 部署脚本已准备
- 🎯 目标服务器: 43.173.170.5
- 👤 用户名: ubuntu

---

## 方案A：手动SSH部署（推荐）

### 步骤1：连接服务器
```bash
ssh ubuntu@43.173.170.5
```

### 步骤2：复制粘贴以下命令（一键部署）
```bash
echo '🚀 开始部署...' && \
sudo apt-get update -qq && \
sudo apt-get install -y nodejs npm git nginx && \
echo '✅ 软件安装完成' && \
sudo mkdir -p /var/www/ai-dungeon && \
sudo chown -R ubuntu:ubuntu /var/www/ai-dungeon && \
if [ -d /var/www/ai-dungeon/.git ]; then 
  cd /var/www/ai-dungeon && git pull origin main
else 
  cd /var/www && git clone https://github.com/EthanJ-Sss/AI-Dungeon.git ai-dungeon
fi && \
echo '✅ 代码已获取' && \
cd /var/www/ai-dungeon/astrocade && \
npm install --legacy-peer-deps && \
echo '✅ 依赖已安装' && \
npm run build && \
echo '✅ 项目已构建' && \
echo 'server {
    listen 80;
    server_name 43.173.170.5;
    root /var/www/ai-dungeon/astrocade/dist;
    index index.html;
    gzip on;
    location / {
        try_files $uri $uri/ /index.html;
    }
}' | sudo tee /etc/nginx/sites-available/ai-dungeon && \
sudo ln -sf /etc/nginx/sites-available/ai-dungeon /etc/nginx/sites-enabled/ && \
sudo rm -f /etc/nginx/sites-enabled/default && \
sudo nginx -t && \
sudo systemctl restart nginx && \
sudo systemctl enable nginx && \
echo '' && \
echo '========================================' && \
echo '✅ 部署完成！' && \
echo '========================================' && \
echo '🌐 访问地址: http://43.173.170.5'
```

### 步骤3：访问
打开浏览器访问: **http://43.173.170.5**

---

## 方案B：分步SSH部署（稳妥）

### 1. 连接服务器
```bash
ssh ubuntu@43.173.170.5
```

### 2. 安装基础软件
```bash
sudo apt-get update
sudo apt-get install -y nodejs npm git nginx
```

### 3. 克隆项目
```bash
sudo mkdir -p /var/www/ai-dungeon
sudo chown -R ubuntu:ubuntu /var/www/ai-dungeon
cd /var/www
git clone https://github.com/EthanJ-Sss/AI-Dungeon.git ai-dungeon
```

### 4. 构建项目
```bash
cd /var/www/ai-dungeon/astrocade
npm install --legacy-peer-deps
npm run build
```

### 5. 配置Nginx
```bash
sudo tee /etc/nginx/sites-available/ai-dungeon << 'EOF'
server {
    listen 80;
    server_name 43.173.170.5;
    root /var/www/ai-dungeon/astrocade/dist;
    index index.html;
    gzip on;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
EOF

sudo ln -sf /etc/nginx/sites-available/ai-dungeon /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl restart nginx
```

### 6. 完成
访问: **http://43.173.170.5**

---

## 方案C：使用部署脚本（自动化）

### 在服务器上运行
```bash
ssh ubuntu@43.173.170.5
```

然后下载并执行部署脚本：
```bash
curl -sL https://raw.githubusercontent.com/EthanJ-Sss/AI-Dungeon/main/deploy-script.sh -o /tmp/deploy.sh
chmod +x /tmp/deploy.sh
/tmp/deploy.sh
```

---

## 预计部署时间
- 软件安装: 2-3 分钟
- 代码克隆: 30 秒
- 依赖安装: 3-5 分钟
- 项目构建: 1-2 分钟
- **总计: 约 7-11 分钟**

---

## 部署后检查

### 1. 检查Nginx状态
```bash
sudo systemctl status nginx
```

### 2. 查看日志
```bash
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### 3. 测试访问
```bash
curl -I http://43.173.170.5
```

---

## 常见问题

### Q: 端口80被占用
```bash
sudo lsof -i :80
sudo systemctl stop apache2  # 如果有Apache
```

### Q: 权限问题
```bash
sudo chown -R www-data:www-data /var/www/ai-dungeon
sudo chmod -R 755 /var/www/ai-dungeon
```

### Q: 构建失败
```bash
cd /var/www/ai-dungeon/astrocade
rm -rf node_modules package-lock.json
npm cache clean --force
npm install --legacy-peer-deps
npm run build
```

---

## ⚠️ 安全提醒

1. **立即修改服务器密码**
   ```bash
   passwd
   ```

2. **配置SSH密钥认证**
   ```bash
   ssh-keygen -t rsa -b 4096
   ssh-copy-id ubuntu@43.173.170.5
   ```

3. **启用防火墙**
   ```bash
   sudo ufw allow 22
   sudo ufw allow 80
   sudo ufw allow 443
   sudo ufw enable
   ```

---

## 需要帮助？

如果遇到问题，请检查：
1. 服务器是否可以访问（ping 43.173.170.5）
2. SSH是否可以连接（ssh ubuntu@43.173.170.5）
3. GitHub仓库是否公开可访问
4. 服务器磁盘空间是否充足（df -h）

---

**推荐方案A** - 最简单快速，一条命令完成！



