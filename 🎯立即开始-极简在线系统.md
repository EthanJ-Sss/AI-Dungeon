# 🎯 立即开始 - 极简在线系统

## ✅ 已完成

我已经为你创建了一个**完全不依赖外部服务**的在线异步对战系统！

### 核心优势
- ✅ **零外部依赖** - 不需要注册 Supabase、Firebase 等任何服务
- ✅ **完全自主** - 数据保存在你自己的服务器 (43.173.170.5)
- ✅ **极简架构** - 只需要 Node.js，使用 JSON 文件存储
- ✅ **即刻部署** - 2-3 个命令即可完成

---

## 🚀 3种方式立即开始

### 🎮 方式1: 本地测试（最快 - 1分钟）

**直接双击运行**:
```
astrocade/🎮本地测试-立即开始.bat
```

这会自动：
1. ✅ 启动后端 API (端口 3001)
2. ✅ 启动前端 (端口 5173)
3. ✅ 打开浏览器

然后你就可以：
- 进入"擂台竞技"
- 输入昵称测试
- 看到完整的在线功能

---

### 📦 方式2: 自动部署到服务器（推荐 - 5分钟）

```bash
cd astrocade
python 🚀自动部署到服务器.py
```

脚本会问你几个问题，然后自动：
1. ✅ 配置环境变量
2. ✅ 构建前端
3. ✅ 上传到服务器 (43.173.170.5)
4. ✅ 启动后端服务

完成后访问: `http://43.173.170.5:8080/`

---

### 🔧 方式3: 手动部署（最灵活 - 8分钟）

#### 步骤1: 准备部署包
```bash
cd astrocade
📦一键构建并准备部署.bat
```

这会在 `astrocade/deploy/` 创建部署包。

#### 步骤2: 上传到服务器

**后端**:
```bash
scp -r ./deploy/backend/* root@43.173.170.5:/root/astrocade-server/
```

**前端**:
```bash
scp -r ./deploy/frontend/* root@43.173.170.5:/var/www/html/
```

#### 步骤3: 启动后端
```bash
ssh root@43.173.170.5

cd /root/astrocade-server
npm install
pm2 start index.js --name ladder-api
pm2 save
pm2 startup
```

#### 步骤4: 完成！
访问 `http://43.173.170.5:8080/`

---

## 📂 已创建的文件

### 后端部分
```
astrocade/
  server/
    index.js           # Node.js 后端 API
    package.json       # 依赖配置
    data/             # 自动创建，存储玩家数据
```

### 前端部分
```
astrocade/
  src/
    services/
      simpleLadderApi.ts          # API 调用层
    store/
      ladderStoreSimple.ts        # 新的在线 Store
    components/
      PlayerRegisterModal.tsx     # 玩家注册弹窗
```

### 部署工具
```
astrocade/
  ⚡一键启动后端.bat                    # 本地启动后端
  🎮本地测试-立即开始.bat                # 本地完整测试
  📦一键构建并准备部署.bat              # 构建部署包
  🚀自动部署到服务器.py                 # 自动化部署脚本
  env-production-template.txt         # 环境配置模板
```

### 文档
```
  ⚡立即开始-3步部署在线系统.md          # 快速开始指南
  ⚡极简部署指南-2步完成.md             # 详细部署指南
  🔗集成说明-使用新的简单API.md         # 前端集成说明
  ✅极简在线系统开发完成-无需外部服务.md  # 完整技术文档
```

---

## 🎯 我推荐你现在做什么

### 推荐流程（最快）

1. **先本地测试**（1分钟）:
   ```bash
   cd astrocade
   双击: 🎮本地测试-立即开始.bat
   ```
   
   验证功能正常后...

2. **部署到服务器**（5分钟）:
   ```bash
   cd astrocade
   python 🚀自动部署到服务器.py
   ```

3. **和朋友一起玩**！
   发送链接: `http://43.173.170.5:8080/`

---

## 📊 数据说明

### 数据存储位置
服务器上的数据保存在: `/root/astrocade-server/data/`

包含2个 JSON 文件:
- `players.json` - 所有玩家数据
- `challenges.json` - 挑战历史记录

### 数据备份
```bash
# SSH 登录服务器后执行
cd /root/astrocade-server
tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz data/
```

### 数据恢复
```bash
tar -xzf backup-YYYYMMDD-HHMMSS.tar.gz
pm2 restart ladder-api
```

---

## 🔧 后端管理

### 查看后端状态
```bash
ssh root@43.173.170.5
pm2 list
```

### 查看日志
```bash
pm2 logs ladder-api
```

### 重启后端
```bash
pm2 restart ladder-api
```

### 停止后端
```bash
pm2 stop ladder-api
```

---

## 🌐 API 端点

后端运行在端口 `3001`，提供以下 API:

- `GET  /api/health` - 健康检查
- `GET  /api/players/check/:playerName` - 检查昵称可用性
- `POST /api/players/register` - 注册新玩家
- `GET  /api/players/:playerName` - 获取玩家信息
- `GET  /api/leaderboard` - 获取排行榜
- `PUT  /api/players/:playerId/defense` - 更新防守阵容
- `POST /api/challenge` - 提交挑战结果
- `GET  /api/challenges/:playerId` - 获取挑战历史

### 测试后端
浏览器访问: `http://43.173.170.5:3001/api/health`

应该看到:
```json
{"status":"ok","message":"异步对战系统运行中"}
```

---

## 🎮 用户体验

### 首次访问
1. 用户访问游戏页面
2. 点击"擂台竞技"
3. 自动检测后端连接
4. 如果连接成功，提示输入昵称注册
5. 如果连接失败，自动降级到本地模式

### 已注册用户
1. 访问页面自动登录
2. 看到实时排行榜
3. 可以挑战其他真实玩家
4. 战绩实时同步

---

## 🐛 故障排除

### 问题1: 本地测试时后端无法启动
**原因**: 端口 3001 被占用

**解决**:
```powershell
# Windows PowerShell
Get-Process -Id (Get-NetTCPConnection -LocalPort 3001).OwningProcess | Stop-Process
```

### 问题2: 前端无法连接后端
**检查**:
1. 后端是否正常运行: `http://localhost:3001/api/health`
2. 浏览器控制台是否有错误
3. 防火墙是否阻止了 3001 端口

**解决**: 前端会自动降级到本地模式，不影响使用

### 问题3: 部署后前端无法访问
**检查**:
1. 前端文件是否上传到正确位置
2. Web 服务器是否正常运行
3. 防火墙是否开放 8080 端口

### 问题4: 部署后无法连接后端 API
**检查**:
1. 后端是否正常运行: `http://43.173.170.5:3001/api/health`
2. 防火墙是否开放 3001 端口
3. `.env.production` 中的 API 地址是否正确

---

## 📚 相关文档

详细信息请查看:

1. **快速开始**: `⚡立即开始-3步部署在线系统.md`
2. **详细部署**: `⚡极简部署指南-2步完成.md`
3. **前端集成**: `🔗集成说明-使用新的简单API.md`
4. **完整文档**: `✅极简在线系统开发完成-无需外部服务.md`

---

## 🎉 立即开始！

**最快测试（1分钟）**:
```bash
cd astrocade
双击: 🎮本地测试-立即开始.bat
```

**最快部署（5分钟）**:
```bash
cd astrocade
python 🚀自动部署到服务器.py
```

**和朋友一起玩吧！** 🎮

