# ✅ 异步对战在线系统部署指南

> 创建日期：2025年10月24日  
> 版本：v1.0 (Supabase MVP)  
> 状态：**就绪**

---

## 📋 系统架构

```
前端 (React + Zustand)
    ↕ HTTPS/REST API
Supabase (后端即服务)
    ├─ PostgreSQL 数据库
    ├─ 实时订阅
    └─ 行级安全策略 (RLS)
```

**特点**：
- ✅ 无需自建后端
- ✅ 免费额度充足（500MB 数据库，50GB 带宽/月）
- ✅ 自动扩展
- ✅ 实时数据同步

---

## 🚀 部署步骤

### 步骤1：创建 Supabase 项目

1. **访问 Supabase**
   - 打开 https://supabase.com
   - 使用 GitHub 账号登录

2. **创建新项目**
   - 点击 "New Project"
   - 填写项目信息：
     ```
     项目名称: astrocade-ladder
     数据库密码: [设置强密码并保存]
     区域: Northeast Asia (Tokyo) - 推荐中国用户
     ```
   - 点击 "Create new project"
   - 等待 1-2 分钟初始化

3. **获取 API 密钥**
   - 进入项目后，点击左侧 "Settings" → "API"
   - 复制以下信息：
     ```
     Project URL: https://xxxxx.supabase.co
     anon public key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
     ```

---

### 步骤2：创建数据库表

1. **打开 SQL Editor**
   - 点击左侧 "SQL Editor"
   - 点击 "New query"

2. **执行数据库脚本**
   - 打开项目文件：`astrocade/supabase-schema.sql`
   - 复制全部内容
   - 粘贴到 SQL Editor
   - 点击 "Run" 执行

3. **验证表创建**
   - 点击左侧 "Table Editor"
   - 应该看到以下表：
     - `ladder_players` (玩家数据)
     - `challenge_records` (挑战记录)
   - 点击 "leaderboard_view" 查看排行榜视图

---

### 步骤3：配置前端项目

1. **创建环境变量文件**
   
   在 `astrocade/` 目录创建 `.env.local` 文件：
   
   ```bash
   # Supabase 配置
   VITE_SUPABASE_URL=https://xxxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```
   
   **注意**：
   - 将 `xxxxx` 替换为你的 Project URL
   - 将 anon key 替换为你的实际密钥
   - `.env.local` 已在 `.gitignore` 中，不会上传到 Git

2. **测试配置**
   
   启动开发服务器：
   ```bash
   cd astrocade
   npm run dev
   ```
   
   - 打开浏览器访问 http://localhost:5173
   - 进入"擂台竞技"
   - 应该看到"在线服务已连接"绿色提示
   - 输入昵称注册

---

### 步骤4：添加初始测试数据（可选）

为了测试排行榜功能，可以添加一些测试玩家：

```sql
-- 在 Supabase SQL Editor 中执行

INSERT INTO ladder_players (player_name, current_rank, defense_formation)
VALUES 
  ('剑圣无敌', 1, '{"totalPower": 5000, "units": []}'),
  ('冰霜女王', 2, '{"totalPower": 4800, "units": []}'),
  ('大地守护者', 3, '{"totalPower": 4600, "units": []}'),
  ('火焰战神', 4, '{"totalPower": 4400, "units": []}'),
  ('风暴猎人', 5, '{"totalPower": 4200, "units": []}');
```

---

## 📊 功能验证清单

### 测试步骤

- [ ] **1. 玩家注册**
  - 进入游戏 → 擂台竞技
  - 看到注册弹窗
  - 输入昵称 "测试玩家001"
  - 点击"加入擂台"
  - 应该成功注册，不再显示弹窗

- [ ] **2. 查看排行榜**
  - 应该看到30名玩家（包括你）
  - 你的排名显示在顶部个人卡片
  - 如果是首次注册，应该显示"未上榜"

- [ ] **3. 设置防守阵容**
  - 点击"设置防守阵容"
  - 拖拽3个角色到网格
  - 点击"保存阵容"
  - 返回擂台页面

- [ ] **4. 挑战对手**
  - 选择排名30的对手
  - 点击"挑战"
  - 确认挑战
  - 进入战斗
  - 战斗结束后查看排名变化

- [ ] **5. 验证数据同步**
  - 在 Supabase Dashboard → Table Editor
  - 查看 `ladder_players` 表
  - 应该能看到你的昵称和排名
  - 查看 `challenge_records` 表
  - 应该有挑战记录

---

## 🔧 常见问题

### Q1: 显示"Supabase 未配置"

**原因**：`.env.local` 文件未创建或配置错误

**解决**：
1. 确认 `.env.local` 文件在 `astrocade/` 目录下
2. 确认变量名是 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`（注意 `VITE_` 前缀）
3. 重启开发服务器（Ctrl+C 然后 `npm run dev`）

---

### Q2: 注册后无法连接服务器

**原因**：Supabase 项目未启动或网络问题

**解决**：
1. 检查 Supabase 项目是否正常运行（访问 Dashboard）
2. 检查浏览器控制台是否有 CORS 错误
3. 确认 Project URL 正确（不要有多余空格）

---

### Q3: 显示"昵称已被占用"

**原因**：该昵称已被其他玩家注册

**解决**：
- 换一个昵称
- 或在 Supabase Table Editor 中删除该记录

---

### Q4: 如何重置所有数据？

**SQL 命令**（在 Supabase SQL Editor 执行）：
```sql
-- 删除所有挑战记录
DELETE FROM challenge_records;

-- 删除所有玩家
DELETE FROM ladder_players;

-- 重置排名
-- （数据已清空，无需额外操作）
```

---

## 📈 生产环境部署

### 部署到 Vercel/Netlify

1. **构建生产版本**
   ```bash
   cd astrocade
   npm run build
   ```

2. **配置环境变量**
   - 在 Vercel/Netlify Dashboard 添加环境变量：
     ```
     VITE_SUPABASE_URL=你的项目URL
     VITE_SUPABASE_ANON_KEY=你的anon密钥
     ```

3. **部署**
   - Vercel: `vercel deploy`
   - Netlify: 拖拽 `dist` 文件夹到 Netlify

---

## 🔐 安全注意事项

### 当前安全级别：基础

**已实现**：
- ✅ 使用 Supabase anon key（只读权限）
- ✅ 行级安全策略（RLS）启用
- ✅ 防止 SQL 注入（使用 Supabase SDK）

**待增强**（v1.1）：
- ⚠️ 用户认证（目前任何人都可以修改数据）
- ⚠️ 战斗结果验证（防止作弊）
- ⚠️ 速率限制（防止刷榜）

### 建议

**MVP 阶段**（当前）：
- 适合小规模测试（<100人）
- 适合朋友间游玩

**正式运营前**：
- 需要添加用户认证系统
- 需要实现战斗结果服务器验证
- 需要添加反作弊机制

---

## 📊 监控和维护

### Supabase Dashboard 功能

1. **实时监控**
   - Dashboard → Database → 查看当前连接数
   - Dashboard → API → 查看 API 调用次数

2. **日志查看**
   - Dashboard → Logs → 查看 API 请求日志
   - 可以看到所有数据库操作

3. **性能优化**
   - Dashboard → Database → Indexes
   - 查看索引使用情况
   - 已创建索引：
     - `current_rank` (排名查询)
     - `player_name` (昵称查询)
     - `challenge_time` (挑战历史)

---

## 🎯 扩展功能路线图

### v1.1 - 用户认证
- [ ] 集成 Supabase Auth
- [ ] 邮箱注册/登录
- [ ] 防止多账号

### v1.2 - 战斗验证
- [ ] 服务器端战斗模拟
- [ ] 结果验证
- [ ] 异常检测

### v1.3 - 赛季系统
- [ ] 赛季自动重置
- [ ] 赛季奖励发放
- [ ] 历史赛季查询

---

## 📁 相关文件

| 文件 | 说明 |
|------|------|
| `supabase-schema.sql` | 数据库结构定义 |
| `src/services/supabase.ts` | Supabase 客户端配置 |
| `src/services/ladderApi.ts` | API 服务层 |
| `src/store/ladderStoreOnline.ts` | 在线模式 Store |
| `src/components/PlayerRegisterModal.tsx` | 注册界面 |
| `.env.local` | 环境变量配置（需手动创建） |

---

## 🎉 完成状态

- [x] Supabase 项目创建指南
- [x] 数据库表结构
- [x] API 服务层
- [x] 前端集成
- [x] 注册流程
- [x] 测试验证清单
- [x] 部署指南
- [x] 常见问题解答

---

**部署就绪！** 🚀

按照本指南操作，约 **15-20 分钟** 即可完成在线系统部署。

**需要帮助？** 查看 Supabase 官方文档：https://supabase.com/docs

---

**创建时间**：2025年10月24日  
**创建者**：Cursor AI Assistant

**祝你的擂台竞技系统运营顺利！** ⚔️✨

